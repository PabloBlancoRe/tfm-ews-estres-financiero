# Artefactos del modelo

Esta carpeta debe contener, descargados desde Colab tras ejecutar la Sección 9 del notebook:

- `lgbm_model.txt`
- `config.json`

Panel de archivos de Colab (icono de carpeta, izquierda) → carpeta `artifacts/` → botón derecho
→ Descargar. Sustituye este README si hace falta, pero no borres la carpeta.
